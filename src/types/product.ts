export interface Product {
  productName: string;
  offerLink: string;
  imageUrl: string;
  price: number;
}

export interface ProductRaw {
  productName: string;
  offerLink: string;
  imageUrl: string;
  price: string;
}